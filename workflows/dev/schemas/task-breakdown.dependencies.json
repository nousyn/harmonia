{
    "jsonFields": [
        {
            "field": "dependencies",
            "required": true,
            "type": "array"
        },
        {
            "field": "executionOrder",
            "required": true,
            "type": "array",
            "minItems": 1
        },
        {
            "field": "parallelGroups",
            "required": true,
            "type": "array"
        }
    ],
    "guidance": "分析任务间的依赖关系，确定执行顺序和可并行的任务组。输出结构化 JSON。"
}
