{
    "jsonFields": [
        {
            "field": "features",
            "required": true,
            "type": "array",
            "minItems": 1
        },
        {
            "field": "constraints",
            "required": true,
            "type": "array"
        },
        {
            "field": "priorities",
            "required": true,
            "type": "object"
        },
        {
            "field": "scope",
            "required": true,
            "type": "string"
        }
    ],
    "guidance": "将用户原始需求结构化为 JSON 格式。只提取需求信息（功能列表、约束、优先级、范围），不做技术分析或方案设计。"
}
